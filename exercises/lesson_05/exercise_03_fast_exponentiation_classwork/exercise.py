"""
Exercise 03 — Fast Exponentiation (Classwork)

TODO: In one sentence, explain why halving n each time gives O(log n)
calls instead of O(n).
"""


def power(x, n):
    """Return x ** n in O(log n) time using divide and conquer."""
    # TODO: base case n == 0
    # TODO: even case -- square power(x, n // 2)
    # TODO: odd case -- x * power(x, n - 1)
    pass
