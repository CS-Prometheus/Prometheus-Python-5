"""Exercise 05 — Count Unique Grid Paths (Homework)"""

from functools import lru_cache


@lru_cache(maxsize=None)
def count_paths(m, n):
    """Return the number of unique right/down paths through an m x n grid."""
    # TODO: base case -- m == 1 or n == 1
    # TODO: recursive case
    pass
